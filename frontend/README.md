# Renovatie frontend

React + TypeScript + Vite frontend for Renovatie. See the [root README](../README.md)
for how to run and deploy the full app.

```bash
yarn dev            # dev server on port 5173, proxies /api to the backend
yarn build          # typecheck + production build
yarn lint           # oxlint
yarn format         # prettier
```
